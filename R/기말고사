1. 반복문 작성
- 반드시 있어야하는 인수
- 생략되면 안되는 인수
num = c(1:20)
for (i in l:length(num)){
      if ( i % 2 != 0) {
            a *= i
      }
}
a

---------------------------------------------
2. 결측치 확인 및 제거
- "in" 활용 코드



---------------------------------------------
3. 이상치 확인 및 제거
- 사용 되는 차트

---------------------------------------------
4. dplyr 패키지 사용법

mpg %>%
      filter (!is.na(mpg$hwy)) %>%
      group_by(dry) %>%
      dplyr::summerise(mean_hwy = mean(hwy))

---------------------------------------------
5.
barplot 활용
      +
ggplot2 활용
      ||
- 막대그래프


stat = 'identity'




