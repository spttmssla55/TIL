1. 반복문 작성
- 반드시 있어야하는 인수
- 생략되면 안되는 인수

v1 = function(f) {
  color = NULL
  for (i in length(peach)){
    if ( peach[i] >= 200 ) {
      color[i] = 'red'
    } else if ( peach[i] >= 180 ) {
      color[i] = 'blue'
    } else {
      color[i] = 'green'
    }
  return color
}

barplot(peach, color = v1(peach) )

---------------------------------------------
2. 결측치 확인 및 제거
- "in" 활용 코드



---------------------------------------------
3. 이상치 확인 및 제거
- 사용 되는 차트

---------------------------------------------
4. dplyr 패키지 사용법

---------------------------------------------
5.
barplot 활용
      +
ggplot2 활용
      ||
- 막대그래프
  ---------------------------------------------
